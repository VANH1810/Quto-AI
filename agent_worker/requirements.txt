# AI Agent worker — TỰ CHỨA (không phụ thuộc backend). Cài trong image agent_worker.
langgraph>=0.2.0
celery[redis]>=5.3        # broker=RabbitMQ (amqp qua kombu), result backend=Redis
redis>=5.0
fastapi>=0.110           # API + Swagger riêng của agent (agent_worker/api.py)
uvicorn[standard]>=0.29
SQLAlchemy>=2.0
asyncpg>=0.29
httpx>=0.27               # gọi Open-Meteo (weather) + Zalo API
pydantic>=2.6
pydantic-settings>=2.2
# LLM provider thật (tuỳ chọn, lazy import): openai / google-generativeai
