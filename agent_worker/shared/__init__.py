"""Shared vendored code — schemas/dữ liệu/tiện ích agent_worker TỰ CHỨA.

Vendor từ backend để agent_worker build & chạy độc lập (context chỉ gồm agent_worker/),
KHÔNG import `app.*`. Đây là bản sao contract; nếu backend đổi schema thì đồng bộ lại.
"""
