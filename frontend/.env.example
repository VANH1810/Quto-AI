# mock | api
NEXT_PUBLIC_DATA_SOURCE=mock
# Bắt buộc là URL HTTPS của backend khi deploy production với dữ liệu thật.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# true chỉ dùng cho bản demo admin độc lập; production thật phải đặt false.
NEXT_PUBLIC_USE_MOCKS=true
