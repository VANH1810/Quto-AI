# Web service (backend/app FastAPI) ONLY — no ML deps here.
# The nowcast/risk pipeline runs offline with its own env: see requirements-tf.txt
# (TensorFlow needs Python <=3.12 and does NOT belong on the Render web service).
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
pyjwt>=2.8
python-multipart>=0.0.9
pytest>=8.0
supabase>=2.4
